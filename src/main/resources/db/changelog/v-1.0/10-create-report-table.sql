create  table report(
                        id bigint primary key generated by default as identity,
                        date_report date,
                        health_status text,
                        photo varchar(255)
);